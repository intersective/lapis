language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1

env:
  - CAKE_VERSION=2 DB=postgres
  - CAKE_VERSION=2 DB=mysql
  - CAKE_VERSION=2.8 DB=postgres

install:
  - composer create-project --no-install cakephp/cakephp:$CAKE_VERSION.* ../cakephp
  - cd ../cakephp && CAKE_DIR=`pwd` && cat $CAKE_DIR/lib/Cake/VERSION.txt
  - cp -R ../lapis plugins/Lapis
  - chmod -R 777 $CAKE_DIR/app/tmp
  - composer global require 'phpunit/phpunit=3.7.38'
  - ln -s ~/.composer/vendor/phpunit/phpunit/PHPUnit $CAKE_DIR/vendors/PHPUnit

before_script:
  - sh -c "mysql -e 'CREATE DATABASE cakephp_test;'"
  - echo "<?php
    class DATABASE_CONFIG {
    public \$test = array(
      'datasource' => 'Database/Mysql',
      'database' => 'cakephp_test',
      'host' => '0.0.0.0',
      'login' => 'travis',
      'persistent' => false,
    );
    }" > ../cakephp/app/Config/database.php

script:
  - ./lib/Cake/Console/cake test Lapis AllTests --stderr
